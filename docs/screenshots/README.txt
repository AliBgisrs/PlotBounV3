Drop the following screenshot files in this folder so the main README renders them.
Filenames must match exactly (case-sensitive on some hosts).

  sidebar-top.png       Sidebar showing Field Setup, Trials & Labels, top of Geometry.
                        Recommended: include a locked origin in meters, multi-trial setup.

  sidebar-bottom.png    Sidebar showing rest of Geometry, Plot Group Editor (enabled),
                        and Colors & Display.

  grid-rendered.png     The map view with red plot polygons and white plot-ID labels
                        rendered on a real orthomosaic. This is the "hero" image — make
                        sure it shows enough plots to convey the workflow.

  origin-locked.png     Close-up of the Field Setup section with the lock button engaged
                        (showing the blue "Locked" badge) and origin displayed in meters.

Optional extras (add to README later if you want):

  orthomosaic-clean.png Raw orthomosaic before any grid is drawn — useful as a "before" shot.
  multi-trial.png       Wider screenshot showing 2+ trials with the trial gap visible.
  serpentine.png        Demonstrates serpentine labeling order.
  plot-group-edit.png   Demonstrates the Plot Group Editor with yellow-edited columns
                        clearly visible alongside red normal columns.

PNG is preferred over JPEG (sharper text, no compression artifacts on UI screenshots).
Target width: 600-900 px for sidebar screenshots, 1000-1400 px for map screenshots.
